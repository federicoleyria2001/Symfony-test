<!DOCTYPE html>

<title>Jugadores</title>

{% block body %}
    <h1>Jugadores</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jugadore.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ jugadore.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ jugadore.apellido }}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ jugadore.ciudad }}</td>
            </tr>
            <tr>
                <th>Pais</th>
                <td>{{ jugadore.pais }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jugadores_index') }}">back to list</a>

    <a href="{{ path('app_jugadores_edit', {'id': jugadore.id}) }}">edit</a>

    {{ include('jugadores/_delete_form.html.twig') }}
{% endblock %}
