<!DOCTYPE html>

<title>Equipo index</title>

{% block body %}
    <h1>Equipo index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for equipo in equipos %}
            <tr>
                <td>{{ equipo.id }}</td>
                <td>{{ equipo.nombre }}</td>
                <td>
                    <a href="{{ path('app_equipo_show', {'id': equipo.id}) }}">show</a>
                    <a href="{{ path('app_equipo_edit', {'id': equipo.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_equipo_new') }}">Create new</a>
{% endblock %}
